30 Nov 2025:
	map is not getting centered to the device location. -> Done
	Increase the route polling rate (more accurate routes) -> Done
    In history page the direction of the travel is not working -> Fixed
    inside the "start new route" page, need to add a back to home page button -> Done
    inside the "start new route" page, remove back route button -> Done
    instead of direclty changing the location of user, make a smooth transition of from the current user location to new gps location (like actually moving)
    remove the points count display in the "start new route" page
	
	direction of the travel is not showing -> Done
	show the distance travelled -> Done
	give option to add check points with different names in a single route. -> Done

	route history page features.
		1. option to go to start point, check points and end points when user selects them. (app should try to show similar path to reach the selected point just like remembering the route that use travelled so that user can return to those points back in the same path) -> Done
		2. show the directions (arrow marks on the route) which user travelled. -> Done
 
    Delete a single history route or select some history routes and Delete them. -> Done
    Add a button to clear all the history routes. -> Done

    Rename the exisiting route's name. -> Done

01 DEC 2025:
  To find another better free alternative of OpenStreetMap (CartoDB Voyager) -> Done
  To migrate local HiveDB to online NoSQL (Firebase) -> Done
  To implement signup and login system -> Done
  To add "save location feature":
    1. A new feature where user should be able to mark the locations and the app should be able to save these 
    marks in the DB and display them in the maps. -> Done
    
    2. Also to add seperate menu to display all those location marks so that whenever user wants to travel to 
    that location he can just click on one of them and the app should be able to show navigation to that location. -> Done

03 DEC 2025:  
  To add a search option in "Explore Map" screen (similar to Google Maps search) -> Done
  Bug: Fix Marker misplacements -> Done
  Refactor the UI -> Done
  Create Settings Screen with following sections: -> Done
    - Profile                   -> Done
    - Units Preferences         -> Done
    - Map Performance           -> Done
    - About                     -> Done
  Implement Background Location Services (to keep tracking while the screen is off) -> Done
  Optimizing battery usage for long trips. -> Done
  Implement Themes (Dark / Light Mode) -> Done
  
04 DEC 2025:  
  Add Loading Symbols to all the Screens      -> Done
  Enhance Search option (Dynamic Searching)   -> Done
  System Testing and Security Testing         -> Done
  Bug fixes                                   -> Done
  Documentation                               -> Done
  Create App Logo and change Splash Screen    -> Done
  Final App Build                             -> Done

	
